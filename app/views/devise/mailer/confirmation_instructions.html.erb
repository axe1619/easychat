<p>Hola <%= @resource.name %>,</p>

<% account_user = @resource&.account_users&.first %>

<% if account_user&.inviter.present? && @resource.unconfirmed_email.blank? %>
  <p><%= account_user.inviter.name %>, de <%= account_user.account.name %>, te invita a probar EasyContact.</p>
<% end %>

<% if @resource.confirmed? %>
  <p>Puedes iniciar sesion en tu cuenta de EasyContact usando el enlace de abajo:</p>
<% else %>
  <% if account_user&.inviter.blank? %>
  <p>
    Bienvenido a EasyContact. Tenemos un conjunto de herramientas listas para ti. Antes necesitamos verificar tu correo electronico para confirmar que eres tu.
  </p>
  <% end %>
  <p>Por favor haz clic en el enlace de abajo para activar tu cuenta.</p>
<% end %>


<% if @resource.unconfirmed_email.present? %>
  <p><%= link_to 'Confirmar mi cuenta', frontend_url('auth/confirmation', confirmation_token: @token) %></p>
<% elsif @resource.confirmed? %>
  <p><%= link_to 'Iniciar sesion en mi cuenta', frontend_url('auth/sign_in') %></p>
<% elsif account_user&.inviter.present? %>
  <p><%= link_to 'Confirmar mi cuenta', frontend_url('auth/password/edit', reset_password_token: @resource.send(:set_reset_password_token)) %></p>
<% else %>
  <p><%= link_to 'Confirmar mi cuenta', frontend_url('auth/confirmation', confirmation_token: @token) %></p>
<% end %>
